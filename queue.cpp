#include<bits/stdc++.h>
using namespace std;
#define endl "\n"
#define int long long
#define Faster ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);

int32_t main(){
    Faster;
    queue<int> q;
    for(int i = 0 ; i < 5; i++){
        q.push(i);
    }

    while (!q.empty())
    {
        cout << q.front() << " ";
        q.pop();
    }
    deque<string> a;
    

      
}